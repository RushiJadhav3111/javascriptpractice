const LifeUpdate = {
    life: 'Unfair',
    GF: 'Geninune',
    freinds: "Gaddari Karabe",
    job: "actually gulami"
}

for (const key in LifeUpdate) {  
//    console.log(`${key} is ${LifeUpdate[key]}`); 
}


const arr = [1, 2, 3, 4, 5]
for (const key in arr) {
    console.log(arr[key]);   
}

